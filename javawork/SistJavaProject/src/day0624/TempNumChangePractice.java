package day0624;

import java.util.Scanner;

public class TempNumChangePractice {

	public static void main(String[] args) {		
		// 수를 입력하고 입력한 수 중 최대와 최소값을 정해라
		
		
		String inputdata;
		int num = 0;
		Scanner sc = new Scanner(System.in);
		int [] data;
		
		while(true) {
			System.out.println("숫자를 입력해주세요(종료를 원하시면 종료를 입력해주세요)");
			//inputdata = sc.nextLine();
			
			
			//if(inputdata.equals("종료"))
			if(num == 0)
				break;
		
			//data = Integer.parseInt(inputdata);
			
			for(int i = 0; i < data.length; i++)
		  	
			 
			/*
			for(int i=0;i<mn.length;i++)
				mn[i]=data;
			int max = mn[0];
			int min = mn[0];
			
			for(int i = 0; i < mn.length; i++) {
				if(max < mn[i]) {
					max = mn[i];
				}
			}
		
			for(int i = 0; i < mn.length; i++) {
				if(min > mn[i]) {
					min = mn[i];
				}
			}
		*/
		//	System.out.println("최대값 : " + max);
		//	System.out.println("최소값 : " + min);
			}
			
		}
	}

